# 第二大脑创建工具：从这里开始

当前界面版本：0.3.2。交付定位：可本地运行，带资产登记配置；真实 GitHub 与资产云联调待完成。

## 第一次使用

将整个文件夹解压到较短的本地路径，例如 `C:\SecondBrain`。不要在压缩包里直接双击文件。

Windows 用户先双击 **首次配置.bat**。电脑需要 Python 3.10 或以上、Git 2.28 或以上；缺少时窗口会告诉你安装位置。首次配置会建立独立的 Python 环境，并联网安装 PyYAML，不修改原来的第二大脑仓库。

配置完成后，双击 **启动向导.bat**。浏览器会打开中文界面。保持启动窗口开启，关闭它就会停止页面服务。

## 平时怎么用

1. 点击“使用示例体验”，先不用想英文名称或写配置文件。
2. 点击“生成创建方案”，检查页面里每个仓库的名称、中文用途和资料位置。
3. 填写审阅者姓名，勾选同意本次创建范围，点击“确认并开始创建”。
4. 在进度页等待实际操作完成。
5. 在验收页逐项看标准与结果，点击“查看领域首页”检查内容。
6. 对内容、导航和使用体验分别记录真实意见，点击“保存验收意见”。
7. 点击“导出验收报告”，将下载的 HTML 文件交给维护者或评审者。双击报告即可阅读，也可用浏览器打印为 PDF。

如觉得难理解，直接选择“需要改进”并写明卡点，不必为了完成任务而选择通过。

## 连续创建与修改需求（0.3.1）

创建完成后，点击结果页的“创建下一个领域”，或页面底部的“返回说明需求 · 新建领域”。页面会回到顶部，清空上一份需求；已经生成的仓库和保存的验收意见保留在“最近记录”中。顶部“＋ 新建领域”也可进入新需求表单。

方案还未执行时，点击“返回修改”可以保留这份方案的需求，修改后重新生成方案。新建领域与修改原方案是两种不同操作。

验收连续创建功能：先创建一个示例并保存意见，再创建一个不同简称的领域；最后从“最近记录”打开第一个，确认首页和意见仍可查看。本地模式的两次创建使用独立工作空间，尚不是向公司同一个在线总入口连续添加领域。

升级时先关闭旧启动窗口，把新版解压到新文件夹，再完成首次配置并启动。默认数据目录没有变化；若维护者曾指定其他数据目录，新版也需使用同一目录才能看到旧记录。不要删除旧数据目录。

## 名称不符合要求时怎么办（0.3.2）

输入框下会直接解释问题，修正后提示会更新。例如 `My Project` 含大写和空格，可以自行改为 `my-project`；`journal` 已用于日志资产类别，不能再作为领域简称。工具不会自动替你改名。

领域简称用于 `quanttide-简称`；英文全名用于 `quanttide-journal-of-英文全名` 等资料仓库。填写正确时可看到名称示例，生成方案后会列出本次全部仓库名称。

章程第五、六条规定仓库名称的组合方式；小写、连字符、简称 60 字符上限和英文全名 72 字符上限属于本工具输入规则。英文长度为标准仓库前缀留出空间，避免填写时合法、生成仓库名后超出执行器限制。简称保留词来自执行规格，用于防止与资产目录重名。

方案页新增四项对照检查：填写与命名格式、仓库名称与创建范围、用途与领域边界、本次操作位置。前两项可由程序检查；后两项需你审阅。创建成功后的七项检查和你的实际体验仍需另外验收。

Windows 试用请按 `quanttide-asset/loops/second-brain-init/Windows试用表.md` 操作，填写实际结果；表中没有预先替你选择通过。

## 你会得到什么

每次本地创建会建立一个独立工作空间：领域和六个配套仓库，共七个；另建立一个测试总入口。资料默认位于用户目录的 `QuanttideSecondBrain`，界面的维护者选项中显示完整路径；启动前可由维护者用 `--storage` 指定位置。

这些文件不会写入你以前的 `second-brain-trial/demo-work`。旧示例仍在那里。首页介绍、目录、连接、许可与变更记录由程序生成，配套仓库先提供 README 骨架，不代表业务应用已经开发完成。

## 三种结果不要混在一起

- 技术检查通过：程序检查了目录、连接、文档和 Git 状态。
- 人工验收通过：你读过内容、理解导航，实际认可使用体验。
- 云端联调通过：在真实 GitHub 或资产云宿主完成操作，本地示例不能证明这一点。

当前向导支持“新建领域”。已有仓库补全、聚合、更名等维护场景仍通过原来的配置入口调用。默认只启用本地模式；真实 GitHub 需要维护者显式开启并完成账号授权，确认时会再次要求输入目标组织名。

## 遇到问题

页面会保留创建记录。刷新浏览器后可从“最近记录”打开任务；关闭服务后重新启动，也能看到历史记录。

执行中途关闭启动窗口可能造成部分操作已完成。重新打开后任务会显示“已暂停”，请先查看原因。临时问题排除后可点“检查并继续”；若出现计划漂移或残留锁提示，请联系维护者，不要删除已创建的仓库来强行重试。

当前会话认证失效时，重新双击启动器并使用它打开的地址。不要把带会话标识的本地地址分享给别人。

## 维护者说明

界面是本机网页工具，监听 127.0.0.1，不是资产云在线站点。入口读取 `quanttide-asset/.quanttide/asset/contract.yaml` 并使用已登记的执行器；不调用资产云 CLI 的归档 run。

已有 Python 环境可运行 `python launch.py`。真实 GitHub 入口通过 `python launch.py --enable-github` 开启；首次正式使用前应在授权的测试组织联调。自动化测试使用 `--test-mode`，报告会标注模拟确认与模拟验收。

本版没有捆绑 Python 或 Git，不是免安装的独立 EXE。Windows 双击脚本与 Python 检查逻辑已提供，真实 Windows 图形流程需要另行记录试用结果。
