# 第二大脑创建工具：从这里开始

当前界面版本：0.5.0，执行器：0.3.0。新增个人 GitHub 登录入口、个人仓库创建、自定义测试总入口、逐步操作记录和可点击的 GitHub 验收链接。真实账号授权、Windows 真人操作和公司资产云联调仍需实际完成并记录；实现了功能不等于完成了这些验收。

## 第一次安装

1. 下载完整 ZIP，解压到较短路径，例如 `C:\SecondBrain050`。不要在压缩包中直接运行。
2. 双击 **首次配置.bat**，按窗口提示准备 Python 3.10 或以上和 Git 2.28 或以上。脚本会建立独立 Python 环境并安装 PyYAML。
3. 双击 **启动向导.bat**，保持启动窗口开启。浏览器右上角应显示 0.5.0。
4. 首先点击“使用示例体验”，在本地创建一轮，熟悉方案、确认、检查和报告。

升级时先关闭旧启动窗口，再解压到新文件夹。默认运行记录在个人用户目录的 `QuanttideSecondBrain`，不会删除旧记录。执行器升级后，旧待执行和暂停计划必须重新调查、生成并确认；不要强行沿用旧确认。旧完成报告可回看，重新检查需匹配原执行器或重新规划。

## 用自己的 GitHub 跑通提交

不必先 Fork 公司第二大脑。第一轮可以让工具新建一个干净的个人测试入口。

1. 在“创建到哪里？”选择 **个人 GitHub（公开测试）**。
2. 点击 **连接 GitHub**。如果提示未安装 GitHub CLI，点击页面提供的官方下载链接，安装 Windows 版本后重新启动本工具。Git 和 GitHub CLI 是两个软件。
3. 页面出现一次性验证码后，点击 **打开 GitHub 官方授权页面**，在 GitHub 页面登录并输入验证码、确认授权。工具可能已经自动打开该页面。授权成功后点击“检查登录状态”，确认显示你的个人账号，例如 `BlackCat205`。
4. 总入口仓库名填写 `second-brain-test`，选择 **第一次：新建测试入口**。这只是建议名称；若已有同名仓库，程序会停止，可换名或在核对后选择使用已有入口。
5. 填写领域需求。第一次可用简称 `poppytrial`、英文全名 `poppytrial-engineering`，并写明“仅用于创建工具测试”。这些也是建议名称，仍要实际检查重名。
6. 点击 **生成创建方案**。核对个人账号、公开属性、8 个目标仓库、命名和用途。生成方案只读取仓库，不创建目标。
7. 填写审阅者姓名，勾选确认，再输入完整目标账号名，点击 **确认并开始创建**。本次会真实创建公开 GitHub 仓库、提交文件、推送并登记总入口。
8. 查看“实际操作记录”：每项标明开始、完成或失败，以及仓库和时间。请求较慢时会继续显示当前动作，不用反复点击创建。
9. 完成后点击“打开 GitHub 核对实际产物”中的链接，打开总入口和领域首页；查看七项检查、提交版本和时间，再保存个人意见并导出报告。

一次性验证码只输入到 GitHub 官方页面。不要向聊天或工具表单粘贴密码和访问令牌。连接 GPT 中的 GitHub 不会自动登录你电脑上的工具。登录成功只表示身份可用，不能提前证明建仓权限或推送成功；最终以实际执行及验收记录为准。

## 再创建第二个领域

点击 **创建下一个领域**。工具清空领域需求，保留运行环境、账号和总入口，并将入口方式切换为“后续：使用已有入口”。填写不同简称和英文全名，再生成方案、确认创建。

GitHub 模式第二次会创建 7 个新仓库并登记到同一个总入口。打开总入口，应同时找到两次创建的领域。退出浏览器后重新操作，请再次检查账号、总入口名称和入口方式。

本地模式仍使用每次独立的测试空间；连续本地创建不等于已向同一个 GitHub 总入口连续登记。

## 如果使用 Fork 的总入口

在你的账号下 Fork 总入口后，选择“使用已有入口”，填写该仓库名。生成方案时展开原有 README、AGENTS 和契约，核对是否允许新增测试领域。Fork 只复制这个仓库，原有子模块和文档链接可能仍指向公司。

工具只新增本次领域并更新入口内的托管目录区块；不会自动搬迁或改写所有公司仓库链接。首次测试推荐干净入口；Fork 兼容性应作为单独一轮测试。当前网页只开放登录者自己的个人公开仓库，组织与私有仓库另行联调。

## 如何判断符合要求

| 看什么 | 人能理解的标准 |
| --- | --- |
| 仓库齐全 | 有领域首页、六个配套仓库和指定总入口 |
| 首页完整 | 能读懂用途、范围、与其他领域的分工 |
| 分类完整 | 有 20 类资料坐标；不要求一次创建 20 个仓库 |
| 连接正确 | 从总入口找到领域，从领域找到六个配套仓库 |
| 资料可追溯 | 新仓库有 README、许可和初始化记录 |
| 提交真实 | GitHub 网站能看到文件、提交和正确的所属账号 |
| 连续使用 | 第二个领域加入同一入口，第一个领域保留 |
| 意见真实 | 如实记录内容是否合理、导航是否清楚、操作是否困难 |

程序检查结构，不替你判断领域内容和英文含义。配套仓库是初始骨架，不代表业务应用已经开发。完整试用表见 [Windows试用表](quanttide-asset/loops/second-brain-init/Windows试用表.md)。

## 只读查询与重名提示

“只读查询 GitHub 现状”独立于创建操作，需要填写调查账号或组织、总入口名称。它通过 Python 调用 GitHub 公开接口，无需 GPT 或登录；按本次需求读取相关仓库、目录和章程，显示查询时间与提交版本，不持续监控所有仓库。

“无法确定”不表示名称可用。404 可能是不存在或不可见；网络错误和限流也不能当作不存在。创建前另行检查，实际重名由 GitHub 拒绝，失败不会自动转成复用已有领域。仓库或账号在计划之后变化，任务会暂停，需重新检查方案。

## 失败后怎么办

页面保留已完成步骤和暂停位置。只有仓库仍匹配上次成功检查点时，“检查并继续”才会继续未完成操作。若部分写入已发生、状态漂移或残留锁，需要维护者检查；不要删除仓库或强行覆盖来重试。多仓库创建没有“一次性全部成功或全部撤销”的保证。

关闭启动窗口会结束本机服务。重新打开后可从最近记录查看已有结果。页面会话失效时，请使用启动器重新打开的地址。

## 维护者信息

工具监听本机 127.0.0.1，默认开放个人 GitHub 入口；`--local-only` 可关闭，`--test-mode` 仅允许本地模拟确认。旧 `--enable-github` 参数兼容保留。

GitHub CLI 管理授权凭证；Git 命令通过本进程的凭证助手设置使用同一 CLI 账号，不改全局 Git 凭证配置。工作副本使用当前登录账号和 GitHub noreply 邮箱作为提交身份。计划绑定账号 ID 与目标归属，执行及每步操作前重新核对。

这不是免安装 EXE，也不是已经部署到资产云的在线插件。公司接入边界见 [上线与联调说明](quanttide-asset/loops/second-brain-init/上线与联调说明.md)。
