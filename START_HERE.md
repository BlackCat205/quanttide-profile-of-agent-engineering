# 第二大脑创建工具：从这里开始

当前界面版本：0.8.3，执行器：0.4.4。此版区分“尚未生成方案”和“执行中断”：前者可在同一记录点击“重新生成方案”，不会再误用连接检查或最终核验。规划已有总入口时，工具按已观察到的提交版本读取 6 个必要文件，不再先完整下载仓库；真正执行时仍会按计划下载和推送。已有用户 Windows/GitHub 试用发现问题，本版修复后的 Windows 真实复验仍待用户完成，契约接入原生实测见 trial-011，最终验收仍由上级判断。

## 第一次安装

1. 下载完整 ZIP，解压到较短路径，例如 `C:\SecondBrain050`。不要在压缩包中直接运行。
2. 双击 **首次配置.bat**，按窗口提示准备 Python 3.10 或以上和 Git 2.28 或以上。脚本会建立独立 Python 环境并安装 PyYAML。
3. 双击 **启动向导.bat**，保持启动窗口开启。浏览器右上角应显示 0.8.3。
4. 首先点击“使用示例体验”，在本地创建一轮，熟悉方案、确认、检查和报告。

升级时先关闭旧启动窗口，再解压到新文件夹。默认运行记录在个人用户目录的 `QuanttideSecondBrain`，不会删除旧记录。执行器升级后，旧待执行和暂停计划必须重新调查、生成并确认；不要强行沿用旧确认。旧完成报告可回看，重新检查需匹配原执行器或重新规划。

## 用自己的 GitHub 跑通提交

不必先 Fork 公司第二大脑。第一轮可以让工具新建一个干净的个人测试入口。

1. 在“创建到哪里？”选择 **个人 GitHub（公开测试）**。
2. 点击 **连接 GitHub**。如果提示未安装 GitHub CLI，点击页面提供的官方下载链接，安装 Windows 版本后重新启动本工具。Git 和 GitHub CLI 是两个软件。
3. 页面出现一次性验证码后，点击 **打开 GitHub 官方授权页面**，在 GitHub 页面登录并输入验证码、确认授权。工具可能已经自动打开该页面。授权成功后点击“检查登录状态”，确认显示你的个人账号，例如 `BlackCat205`。
4. 总入口仓库名填写 `second-brain-test`，选择 **第一次：新建测试入口**。这只是建议名称；若已有同名仓库，程序会停止，可换名或在核对后选择使用已有入口。
5. 填写领域需求。第一次可用简称 `poppytrial`、英文全名 `poppytrial-engineering`，并写明“仅用于创建工具测试”。这些也是建议名称，仍要实际检查重名。
6. 点击 **生成创建方案**。核对个人账号、公开属性、8 个目标仓库、命名和用途。生成方案只读取仓库，不创建目标。
7. 填写审阅者姓名，勾选确认，再输入完整目标账号名，点击 **确认并开始创建**。本次会真实创建公开 GitHub 仓库、提交文件、推送并登记总入口。
8. 查看“实际操作记录”：每项标明开始、完成或失败，以及仓库和时间。请求较慢时会继续显示当前动作，不用反复点击创建。
9. 完成后点击“打开 GitHub 核对实际产物”中的链接，打开总入口和领域首页；查看七项检查、提交版本和时间，再保存个人意见并导出报告。

一次性验证码只输入到 GitHub 官方页面。不要向聊天或工具表单粘贴密码和访问令牌。连接 GPT 中的 GitHub 不会自动登录你电脑上的工具。登录成功只表示身份可用，不能提前证明建仓权限或推送成功；最终以实际执行及验收记录为准。

## 缺少 GitHub CLI 时如何安装（Windows）

安装 GitHub CLI 不需要打开 CMD，它与 Git 是两个软件。

1. 打开 [GitHub CLI 官网](https://cli.github.com/)，在下载选项中选择 **Windows — Download MSI**，下载 `.msi` 安装包。普通 Intel/AMD 64 位 Windows 电脑使用 `windows_amd64.msi`；ARM 电脑需选择相应 ARM64 安装包。
2. 双击下载的文件，按安装向导完成安装，通常依次点击 Next、Install、Finish。
3. 关闭本工具原来的启动窗口和网页，再双击 **启动向导.bat**。只刷新旧页面不够，因为旧启动进程可能尚未读取新安装路径。
4. 在新页面选择“个人 GitHub”，点击“检查登录状态”；显示尚未登录时，再点击“连接 GitHub”完成官方授权。

如果仍提示缺少，可按 **Win + R**，输入 `cmd` 并回车，在新窗口中输入：

```bat
gh --version
```

出现 `gh version ...` 表示安装可用。若显示“不是内部或外部命令”，尝试重启电脑后重新打开工具；仍不行则保留提示文字供排查。CMD 仅用于这一步可选检查，日常创建在网页完成。

## 再创建第二个领域

点击 **创建下一个领域**。工具清空领域需求，保留运行环境、账号和总入口，并将入口方式切换为“后续：使用已有入口”。填写不同简称和英文全名，再生成方案、确认创建。

GitHub 模式第二次会创建 7 个新仓库并登记到同一个总入口。打开总入口，应同时找到两次创建的领域。退出浏览器后重新操作，请再次检查账号、总入口名称和入口方式。

本地模式仍使用每次独立的测试空间；连续本地创建不等于已向同一个 GitHub 总入口连续登记。

## 如果使用 Fork 的总入口

在你的账号下 Fork 总入口后，选择“使用已有入口”，填写该仓库名。生成方案时展开原有 README、AGENTS 和契约，核对是否允许新增测试领域。Fork 只复制这个仓库，原有子模块和文档链接可能仍指向公司。

工具只新增本次领域并更新入口内的托管目录区块；不会自动搬迁或改写所有公司仓库链接。首次测试推荐干净入口；Fork 兼容性应作为单独一轮测试。当前网页支持本人账号或本人担任所有者的测试组织，仅支持公开仓库。

## 如何判断符合要求

| 看什么 | 人能理解的标准 |
| --- | --- |
| 仓库齐全 | 有领域首页、六个配套仓库和指定总入口 |
| 首页完整 | 能读懂用途、范围、与其他领域的分工 |
| 分类完整 | 有 20 类资料坐标；不要求一次创建 20 个仓库 |
| 连接正确 | 从总入口找到领域，从领域找到六个配套仓库 |
| 资料可追溯 | 新仓库有 README、许可和初始化记录 |
| 提交真实 | GitHub 网站能看到文件、提交和正确的所属账号 |
| 连续使用 | 第二个领域加入同一入口，第一个领域保留 |
| 意见真实 | 如实记录内容是否合理、导航是否清楚、操作是否困难 |

程序检查结构，不替你判断领域内容和英文含义。配套仓库是初始骨架，不代表业务应用已经开发。完整试用表见 [Windows试用表](quanttide-asset/loops/second-brain-init/Windows试用表.md)。

## 只读查询与重名提示

“只读查询 GitHub 现状”独立于创建操作，需要填写调查账号或组织、总入口名称。它通过 Python 调用 GitHub 公开接口，无需 GPT 或登录。生成真实 GitHub 创建方案时，改用电脑上已登录的 GitHub CLI 调用认证 API；两者都按本次需求读取相关仓库、目录和章程，显示查询时间与提交版本，不持续监控所有仓库。

“无法确定”不表示名称可用。404 可能是不存在或不可见；网络错误和限流也不能当作不存在。创建前另行检查，实际重名由 GitHub 拒绝，失败不会自动转成复用已有领域。仓库或账号在计划之后变化，任务会暂停，需重新检查方案。

## 失败后怎么办

页面保留已完成步骤和暂停位置。只有仓库仍匹配上次成功检查点时，“检查并继续”才会继续未完成操作。若部分写入已发生、状态漂移或残留锁，需要维护者检查；不要删除仓库或强行覆盖来重试。多仓库创建没有“一次性全部成功或全部撤销”的保证。

如果页面明确显示“尚未生成创建方案”，表示还没有开始创建或推送仓库。此时点击“重新生成方案”即可沿用原填写内容和原记录重试；不要使用“检查并继续”或“重新检查最终结果”。新版会把右侧 GitHub 连接检查置灰，避免再次寻找不存在的 `execution-plan.json`。

关闭启动窗口会结束本机服务。重新打开后可从最近记录查看已有结果。页面会话失效时，请使用启动器重新打开的地址。

## 维护者信息

工具监听本机 127.0.0.1，默认开放个人 GitHub 入口；`--local-only` 可关闭，`--test-mode` 仅允许本地模拟确认。旧 `--enable-github` 参数兼容保留。

GitHub CLI 管理授权凭证；Git 命令通过本进程的凭证助手设置使用同一 CLI 账号，不改全局 Git 凭证配置。工作副本使用当前登录账号和 GitHub noreply 邮箱作为提交身份。计划绑定账号 ID 与目标归属，执行及每步操作前重新核对。

这不是免安装 EXE，也不是已经部署到资产云的在线插件。公司接入边界见 [上线与联调说明](quanttide-asset/loops/second-brain-init/上线与联调说明.md)。


## 从旧版升级并恢复 sample 任务

1. 下载 0.8.3 版，解压到一个新的文件夹。关闭旧工具的启动窗口和页面，再从新版双击“启动向导.bat”。不要删除电脑上的 QuanttideSecondBrain 文件夹。
2. 确认右上角显示 0.8.3。默认保存位置不变，“最近记录”会显示原任务。若以前自定义过保存位置，启动新版时也需要使用同一位置。
3. 打开暂停的示例工程，点击“检查补交方案”。工具只支持已完成全部创建步骤、全新总入口唯一残留模板 LICENSE 的已知问题；其他变化会停止处理。
4. 阅读目标账号、仓库和许可内容，勾选确认，再点“确认补交并核验”。这会新增一个仅包含 LICENSE 的提交，不重建仓库，不改写原失败记录。
5. 如果显示“补交完成”但远端仍无法核验，恢复网络后点击“重新检查”。如果推送响应丢失，可继续同一补交；程序先核对版本，避免生成重复提交。出现额外文件或版本变化时，不要自行清理，导出诊断包交给维护者。

旧版未执行的计划需返回填写并重新生成；旧版未完成的其他中断任务不支持直接跨版本继续，保留旧版程序和记录供维护者处理。

## 看懂进度和检查结果

- “创建与提交”统计建仓、文件写入和推送等动作，完成不代表验收通过。
- “验收”逐项核对文件、目录、仓库连接及本地和远端提交，并显示当前仓库和时间。
- **通过**：该次检查有证据支持；**发现问题**：例如具体文件没有提交；**暂时无法核验**：远端读取失败，不能判定产物不合格。
- 同一轮涉及多次网络访问，一次成功不保证所有访问成功；查询是本次范围的即时检查，不是量潮全部仓库的持续监控。

## 不用命令行提供排查资料

在页面的“记录与排查”区域点“查看保存位置”，可看到完整路径和打开方法。打开要排查的历史任务，等操作停止后点“导出本次诊断包”，把下载的 ZIP 交给维护者。包内包含仓库名称、步骤、版本和检查时间，不包含需求正文、仓库文件、登录凭据或完整本机路径。保留原记录，诊断包不用于导入或恢复执行。

## 非技术同事验收顺序

先核对“创建到哪里”和方案：总入口负责导航，新领域与六个配套仓库分别存放资料，首次共八个仓库。命名提示只证明格式符合，公开查询未发现仓库不代表名称必然可用。确认后创建；技术检查全部通过，再打开领域首页检查用途和边界，查看总入口能否找到该领域，最后如实填写三项人工意见。之后点“创建下一个领域”可复用已选总入口，历史记录保留。

## 按上级要求接入与试用

本任务采用 .quanttide 契约登记，不要求另做资产云网站或接口。最新版登记已用原生 CLI 实测，证据见 [trial-011](quanttide-asset/loops/second-brain-init/trials/trial-011.md)。识别登记不等于 GitHub 创建验收通过，也不是上级已评价通过。

如需使用自己的测试组织：先连接个人账号，在“我管理的测试组织”填写组织名；工具会确认你是所有者，再生成方案。组织必须已经存在，工具不代为创建。留空仍使用个人账号。审阅时核对目标归属，确认栏输入方案中的目标账号或组织名。

先用不同于 sample 的新领域名称和全新测试入口完成正常创建，再向该入口追加第二个领域。只有测试量潮现有资料兼容时才需要相关仓库副本；不必为契约识别测试复制整个第二大脑。旧 sample 的错误记录保留，不标记成成功；可导出诊断包继续排查。

0.8.3 会将旧报告中的远端读取失败显示为“暂时无法核验”，并明确标注历史报告，不改写原日志。补交预检查失败的原因也会进入诊断包。

## 0.8.2 中断恢复

如果你已经创建过 sample3，不要换名重建。请从最近记录打开原任务，按照 [断网恢复说明](quanttide-asset/loops/second-brain-init/断网恢复说明.md) 操作。新版执行器为 0.4.3；若页面显示 27/27 项已完成，使用“重新检查”，不要再执行创建步骤。其他旧任务不会自动改写计划或绕过版本检查。

## 0.8.3 方案生成恢复

若历史记录的错误中出现 `execution-plan.json` 不存在，表示旧界面让“未生成方案”的记录调用了依赖方案的按钮，并不表示文件被误删。打开同一条记录，点击“重新生成方案”。这一步只重新调查和形成待审阅方案，不创建仓库；看到核对页面并人工确认后，才会开始真实写入。

规划已有总入口时，工具先记录远端提交号，再从该提交读取 README、AGENTS、`.gitmodules` 和三类 `.quanttide` 契约。可选文件返回明确的 404 时按不存在处理；网络错误、限流或版本变化仍会暂停，不能冒充“文件不存在”。
