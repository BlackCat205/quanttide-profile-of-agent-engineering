# UI 0.3.2 命名提示与创建前检查

日期：2026-09-07。环境：Linux，Chrome Headless Shell 151.0.7922.34，Python 服务端与真实本地 Git 操作。用户确认与反馈为自动化模拟。

## 变更与依据

- 输入框旁说明错误，合法名称展示领域仓库和日志仓库示例，不自动改写用户命名。
- 字符规则与保留词读取现有执行器和规格，后端同时验证输入，防止绕过网页校验。
- UI 限制简称最多 60 字符；英文全名最多 72 字符，按最长标准仓库前缀和执行器小于 100 字符的限制计算。该长度是工具约束，不标为章程原文。
- 章程第五、六条提供仓库名称组合依据；方案页新增格式、创建范围、含义与边界、操作位置四项检查，后两项明确需要人工确认。
- 补充真实 Windows 试用空表与提交评审说明。

## 实际测试

浏览器 37 项检查通过，详见 [browser-result.json](ui-browser-032/browser-result.json)。新增用例覆盖大写与空格、下划线、首位连字符、重复连字符、保留词、中文；确认不合格输入不会建立计划或仓库。验证名称预览、四项对照清单，随后执行两个领域的实际本地创建并回看第一份记录。截图在同目录。

服务端与执行流程 8 项测试通过，用时 46.477 秒，详见 [tests.txt](ui-tests-032/tests.txt)。新增直接 HTTP 提交错误类型、超长名称与缺失内容的拦截；长度边界还核对生成后的标准仓库名可通过执行器校验。原有确认、失败报告、内容变化后验收失效、文档访问范围与重启恢复测试同时通过。

完成 JavaScript/Python 语法检查及 Git 差异空白检查。未重跑独立的全套核心 CLI 测试，本轮执行器代码未修改。

## 未完成事项

Windows 真人试用、上传个人 GitHub、真实 GitHub 创建、资产云宿主调用均未完成。请按上级目录 Windows试用表.md 记录真实使用情况；自动化通过不代表用户一定理解规范。
