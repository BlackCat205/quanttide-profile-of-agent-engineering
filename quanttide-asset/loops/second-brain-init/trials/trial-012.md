# 0.6.2 章程读取故障修复记录

日期：2026-09-09。环境：Linux；未在用户 Windows 实机验证。

## 问题与修改

- 原提示把章程确实变更与网络读取失败合并，用户无法判断下一步。
- 现在分别提示已确认变化与暂时无法核对，并列明采用版本/在线版本及 HTTP 状态（若有）。未知状态仍阻止执行，不绕过章程检查。
- 公开只读调查的临时网络/服务器错误最多请求三次；401/403/404/429 不自动重试。
- 诊断包新增章程版本、失败原因、调查起止时间及执行前规则核对，排除章程正文。
- 创建未完成时隐藏整个 LICENSE 补交面板。
- 引擎及规则文件未修改，保持既有任务版本约束。

## 核对与测试

通过 GitHub 连接器读取章程 contract/public-second-brain.md 默认分支及采用提交 0301294afc35297e2668119eb00248345ff55309，两份文件 blob SHA 均为 48204db8d798dd7096371e4df8b7f53987dea7be。该观察仅证明本次读取内容一致，不能还原用户失败时的网络情况。

- test_survey_errors.py：6 项通过；覆盖未知与变化区分、诊断正文排除、有限重试和权限/限流不重试。
- test_github.py：10 项通过（模拟 GitHub，本地 Git 工作流）。首次运行发现旧测试基线仅记录 SHA 而无 status；执行前判断恢复兼容已有 SHA 基线后通过。
- test_ui.py：8 项通过。
- test_recovery_browser.cjs：补充无计划暂停时隐藏补交入口的检查，但本环境浏览器启动 SIGSEGV，未完成，不计通过。
- git diff --check 通过。

## 用户试用

下载 0.6.2，关闭旧启动窗口并启动新版，保留原工作空间。
若此次尚未生成计划，选择返回填写、重新生成方案；已有执行计划则按页面提供的检查并继续。
若再次暂停，导出本次诊断包交给维护者。不需要删除原仓库，不使用 LICENSE 补交来修复章程读取。

## 尚未解决的验证边界

未取得本次用户端新诊断包，具体网络失败原因尚待确认。当前读取仍为 Python 访问公开 GitHub API，不使用 GPT；Git 的 HTTP/1.1 配置不会改变该读取通道。未进行真实组织仓库写入或 Windows 完整验收。
